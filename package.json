{
  "name": "google-image-downloader",
  "version": "1.0.0",
  "description": "A Node.js script that downloads images from Google based on a search query, resizes them, and stores them securely in a PostgreSQL database. Designed with asynchronous processing for efficiency and Docker support for easy deployment.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "pg": "^8.13.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
